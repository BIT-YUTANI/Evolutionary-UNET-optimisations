The main structure of flow of code currently is to


initial base 
    | 
middle base <--> middle base (how many gens we want in multiples of 5)
    |
optuna layer
    |
Training and visualisation layer


The output checkpoints of previous becomes the input dataset for the next notebook.
